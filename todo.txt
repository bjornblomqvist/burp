
  - Fix multiple sections 
    - Add cleanup to the contentDecorator
    - Add the selector showing all the section names
    
    - On selector change
      - Cleanup old
      - New Content decorator
      - Set the editor value
      
    - Fix so that we can create switch contentDecorator
  
  - Let Christer write stuff for it.
    
  - Fix so that we don't need a container.
  
  - Fix so that we use a iFrame
  