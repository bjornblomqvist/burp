

<%= twitter_bootstrap_form_for :group, :url => "./", :method => (@group ? :put : :post),:html => {:id => "edit-group-form",:class => 'form-horizontal'} do |f| %>

  <%= f.fieldset "Group" do %>
    <%= f.text_field :name, "Name" %>
  <% end %>
  
  
  <%= f.actions do %>
    <%= f.submit @group ? 'Save' : "Create" %>
    <%= button_tag  'Cancel', :onclick => "document.location = '#{menu_path(@menu)}'; return false;", :class => 'btn' %>
    <% if @group %>
      <%= button_tag  'Remove', :onclick => "$('#remove-link').click(); return false;", :class => 'btn' %>
      <%= link_to 'Destroy', menu_group_path(@menu,@group), :confirm => 'Are you sure?', :id => 'remove-link', :method => :delete, :style => "display: none;" %>
    <% end %>
  <% end %>
<% end %>
